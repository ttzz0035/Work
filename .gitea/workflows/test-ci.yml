name: Test Gitea CI

on: [push]

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Print Hello Message
        run: |
          echo "✅ Gitea Actions is working!"
          echo "Repository: $GITEA_REPOSITORY"
          echo "Run ID: $GITEA_ACTION_RUN_ID"
          echo "Triggered by: $GITEA_ACTOR"

      - name: Test Secret Access
        env:
          TEST_SECRET: ${{ secrets.TEST_SECRET }}
        run: |
          if [ -z "$TEST_SECRET" ]; then
            echo "⚠️  No TEST_SECRET defined."
          else
            echo "🔐 TEST_SECRET found (length: ${#TEST_SECRET})"
          fi

